// Production environment, swapped in at build time via angular.json
// fileReplacements (configuration "production").
//
// apiUrl must point at the deployed Render API. Update this to your actual
// Render service URL before the production build/deploy.
export const environment = {
  production: true,
  apiUrl: 'https://oinkbooks-api.onrender.com',
};
