// Dev environment. For prod, add environment.prod.ts + a fileReplacements
// entry in angular.json so `apiUrl` points at the deployed API.
export const environment = {
  production: false,
  apiUrl: 'http://localhost:3000',
};
